{
  "name": "mcp-tool-bridge",
  "owner": {
    "name": "Hibryda"
  },
  "metadata": {
    "description": "MCP server wrapping CLI tools (ls, wc, diff, find, lsof, ps, git, gh_api, kubectl, docker, sqlite, curl, repo_snapshot, pr_status) with structured JSON output, plus a PreToolUse hook.",
    "version": "0.2.0"
  },
  "plugins": [
    {
      "name": "mcp-tool-bridge",
      "source": "./plugin",
      "description": "22-tool MCP server with PreToolUse hook for Claude Code",
      "category": "productivity",
      "tags": [
        "mcp",
        "rust",
        "git",
        "kubectl",
        "docker",
        "structured-output"
      ]
    }
  ]
}
